---
name: {{ cookiecutter['Action Name'].lower().replace(' ', '-') }}
description: {{ cookiecutter['Description'] }}
runner_type: run-python
enabled: true
entry_point: {{ cookiecutter['Entry Point'].lower().replace(' ', '-') }}
parameters:
  # List all parameters you want this action to have
  # action:
  #   default: build_base_http_request
  #   type: string
